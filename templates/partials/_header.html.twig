<nav class="navbar navbar-expand-lg bg-success">
	<div
		class="container-fluid">
		{# Logo #}
		<a class="navbar-brand" href="{{ path('app_home') }}">
			Jardin partagé
			<img src="{{asset('images/logo.png')}}" height="40">
		</a>

		{# Burger pour le responsive #}
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Basculer la navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-end w-100">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_parcelle_index') }}">Ma Parcelle</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_logout") }}">Déconnexion</a>
					</li>
					{% if is_granted("ROLE_ADMIN") %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Administration
							</a>
							<ul class="dropdown-menu dropdown-menu-end">
								<li>
									<a class="dropdown-item" href="{{ path('app_admin_parcelle_index') }}">Parcelles</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('app_admin_plante_index') }}">Plantes</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('app_admin_user_index') }}">Utilisateurs</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('app_admin_post_index') }}">Posts</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('app_admin_comment_index') }}">Commentaires</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('app_reunion_index') }}">Réunions</a>
								</li>
							</ul>
						</li>
					{% endif %}
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_login") }}">Connexion</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_register") }}">S'inscrire</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
