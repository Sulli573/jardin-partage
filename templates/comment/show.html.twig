{% extends 'base.html.twig' %}

{% block title %}Commentire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ comment.message }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home') }}">Retour à la page d'accueil</a>

    <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">Modifier</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
