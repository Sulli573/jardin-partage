<form method="post" action="{{ path('app_plante_delete', {'id': plante.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette plante?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plante.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
