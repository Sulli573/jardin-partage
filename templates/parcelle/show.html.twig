{% extends 'base.html.twig' %}

{% block title %}Parcelle{% endblock %}

{% block body %}
    <h1>Parcelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parcelle.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ parcelle.number }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ parcelle.size }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ parcelle.createdAt ? parcelle.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parcelle_index') }}">back to list</a>

    <a href="{{ path('app_parcelle_edit', {'id': parcelle.id}) }}">edit</a>

    {{ include('parcelle/_delete_form.html.twig') }}
{% endblock %}
