{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{# 
    {{ ... }}, utilisé pour afficher le contenu d'une variable ou le résultat de l'évaluation d'une expression ;
    {% ... %}, utilisé pour exécuter une logique, telle qu'une condition ou une boucle ;
    {# utilisé pour ajouter des commentaires au modèle (contrairement aux commentaires HTML, ces commentaires ne sont pas inclus dans la page rendue).
#}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<div class="example-wrapper">
    {# Peux ajouter plusieurs filtres les uns a la suite des autres avec | (alt gr + 6) #}
    {# <h1>Hello {{ controller_name|upper }}! ✅</h1> #}
    {# 
        {% set nombre = 2 %} Ajoute une variable

        {% if nombre < 0 %}
            Je suis inf à 0 pas de echo en sympfony
        {% elseif nombre > 0 %}
            Je suis sup à 0
        {% else %}
            Je sais pas
        {% endif %} 
    #}

    {# <img src="{{ asset('images/flamingo-7183937_640.jpg') }}" alt="image"> #}
</div>
{% endblock %}
